"use strict";function playSound(e){const t=document.querySelector(`audio[data-key="${e.keyCode}"]`),n=document.querySelector(`.key[data-key="${e.keyCode}"]`);t&&(t.currentTime=0,t.play(),n.classList.add("playing"))}function removeTrasition(e){"transform"===e.propertyName&&this.classList.remove("playing")}window.addEventListener("keydown",playSound);const keys=this.document.querySelectorAll(".key");keys.forEach(e=>e.addEventListener("transitionend",removeTrasition));